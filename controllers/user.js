const db = require("../models");







module.exports = {
    registerUser: async (req,res,next)=>{
        try {
            
        } catch (error) {
            return next(error);
        }
    }
}